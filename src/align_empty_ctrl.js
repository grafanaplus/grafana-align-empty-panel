import {PanelCtrl} from 'app/plugins/sdk';

export class AlignEmptyCtrl extends PanelCtrl {
  constructor($scope, $injector) {
    super($scope, $injector);
  }
}

AlignEmptyCtrl.templateUrl = 'module.html';
